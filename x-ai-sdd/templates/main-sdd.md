# 主 SDD：服务/能力/域名称

> 主 SDD 用于一个**服务边界 / 业务域 / 数据层 / 事件协议**这种长期维护的对象。
> 主 SDD 不写具体功能的流水；功能流水写在 `features/active/*.md`。

## 0. 文档状态

| 字段 | 内容 |
| --- | --- |
| 状态 | draft |
| 当前阶段 | 待补充 |
| 适用范围 | 待补充（例：`backend/order-service` 全部） |
| 负责人 | 待补充 |
| 最后更新 | YYYY-MM-DD |

## 1. 本 SDD 负责什么

> 服务边界、业务域范围。

## 2. 本 SDD 不负责什么

> 容易混淆的相邻服务/域，注明由哪份 SDD 负责。

## 3. 当前阶段目标

> 本服务/域当前阶段的核心目标（重构期 / 稳态 / 弃用中）。

## 4. 输入来源

> 上游调用方、消息来源、数据源。

## 5. 输出结果

> 对外接口、对外事件、对下游的数据写入。

## 6. 核心流程

> 服务级核心链路骨架。具体功能链路在 Feature SDD 内细化。

## 7. 核心规则

> 服务级通用业务规则与不变式（例：所有分页查询必须给默认排序）。
> 此处的规则会被所有 Feature SDD 继承。

## 8. 涉及对象

> 主要领域对象与数据模型。涉及两张及以上关联表、跨库逻辑键、
> checkpoint/租约/Outbox/delivery/失败状态表时，必须在本节或 §9 提供 Mermaid
> `erDiagram`。跨 database/schema 关联须注明“逻辑关联，非物理外键”。

## 9. 数据或字段要求

> 公共字段约束、命名规范。每张新增或修改表的数据字典至少覆盖以下项目；索引可单独成表。

| 字段 | 数据库类型 | 可空 | 默认值 | 键/约束 | 索引 | 用途 | 数据来源 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `…` | `…` | 是/否 | `…` | PK / UK / FK / CHECK / 逻辑关联 | `…` | `…` | `…` |

> 图示不能替代字段表、规范性 DDL、索引和迁移说明。

## 10. 幂等规则

> 服务级幂等约定（例：所有写接口必须接受 `X-Request-Id`）。

## 11. 状态或生命周期规则

> 主要领域对象的状态机。画图用 mermaid `stateDiagram-v2`，不要用 ASCII 框线图。

## 12. 审计或事件规则

> 本服务/域发布的事件列表、命名、版本策略。

## 13. 异常和人工处理

> 服务级失败处理、降级策略、人工补偿入口。

## 14. 与其他 SDD 的调用关系

> 服务/域之间的依赖图。画图用 mermaid `flowchart`，不要用 ASCII 框线图。

## 15. 验收标准

> 服务级公共验收项（例：所有接口必须返回 trace_id；所有事件必须支持幂等消费）。

- [ ] …

## 16. 决策记录

| 编号 | 决策 | 备选 | 选择理由 | 时间 |
| --- | --- | --- | --- | --- |

## 17. 待确认问题

- [ ] …

## 附录 A：本服务/域下的 Feature SDD 索引

| 状态 | 名称 | 路径 |
| --- | --- | --- |
| ready | 订单列表筛选 | `feats/03-订单列表筛选/README.md` |
| accepted | 报告数据接入 | `feats/02-报告数据接入/README.md` |
